<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - Panel Admin API Keys</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        :root {
            --primary-bg: #000000;
            --secondary-bg: #1a1a1a;
            --accent-purple: #8b5cf6;
            --accent-cyan: #06b6d4;
            --text-primary: #ffffff;
            --text-secondary: #a1a1aa;
            --border-color: #374151;
        }
        
        body {
            background: radial-gradient(circle at center, var(--secondary-bg) 0%, var(--primary-bg) 100%);
            color: var(--text-primary);
            font-family: 'Courier New', monospace;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        
        .login-container {
            background: var(--secondary-bg);
            border: 2px solid var(--accent-purple);
            border-radius: 20px;
            box-shadow: 0 0 30px rgba(139, 92, 246, 0.3);
            padding: 2rem;
            width: 100%;
            max-width: 400px;
            animation: glow 2s ease-in-out infinite alternate;
        }
        
        @keyframes glow {
            from {
                box-shadow: 0 0 30px rgba(139, 92, 246, 0.3);
            }
            to {
                box-shadow: 0 0 40px rgba(139, 92, 246, 0.6);
            }
        }
        
        .neon-white {
            color: #ffffff;
            text-shadow: 
                0 0 5px #ffffff,
                0 0 10px #ffffff,
                0 0 15px #ffffff,
                0 0 20px #8b5cf6,
                0 0 35px #8b5cf6,
                0 0 40px #8b5cf6;
        }
        
        .neon-purple {
            color: #8b5cf6;
            text-shadow: 
                0 0 5px #8b5cf6,
                0 0 10px #8b5cf6,
                0 0 15px #8b5cf6,
                0 0 20px #8b5cf6;
        }
        
        .form-control {
            background: var(--primary-bg);
            border: 1px solid var(--border-color);
            color: var(--text-primary);
            border-radius: 10px;
        }
        
        .form-control:focus {
            background: var(--primary-bg);
            border-color: var(--accent-purple);
            color: var(--text-primary);
            box-shadow: 0 0 15px rgba(139, 92, 246, 0.4);
        }
        
        .btn-primary {
            background: linear-gradient(135deg, var(--accent-purple) 0%, var(--accent-cyan) 100%);
            border: 1px solid var(--accent-purple);
            border-radius: 10px;
            color: var(--text-primary);
            box-shadow: 0 0 15px rgba(139, 92, 246, 0.3);
            transition: all 0.3s;
        }
        
        .btn-primary:hover {
            box-shadow: 0 0 25px rgba(139, 92, 246, 0.6);
            transform: translateY(-2px);
        }
        
        .alert-danger {
            background: rgba(220, 38, 38, 0.1);
            border: 1px solid #dc2626;
            color: var(--text-primary);
            border-radius: 10px;
        }
        
        .support-info {
            background: rgba(139, 92, 246, 0.1);
            border: 1px solid var(--accent-purple);
            border-radius: 10px;
            padding: 1rem;
            margin-top: 1rem;
        }
        
        .support-links a {
            color: var(--accent-cyan);
            text-decoration: none;
            margin: 0 10px;
            transition: all 0.3s;
        }
        
        .support-links a:hover {
            color: var(--text-primary);
            text-shadow: 0 0 10px var(--accent-cyan);
        }
    </style>
</head>
<body>
    <div class="login-container">
        <div class="text-center mb-4">
            <h2 class="neon-white mb-2">
                <i class="fas fa-shield-alt"></i> WOLFDATA-DOX
            </h2>
            <p class="neon-purple">Panel de Administración</p>
        </div>
        
        <% if (error) { %>
            <div class="alert alert-danger" role="alert">
                <i class="fas fa-exclamation-triangle me-2"></i>
                <strong>Acceso Denegado</strong><br>
                <%= error %><br><br>
                <div class="support-info">
                    <h6><i class="fas fa-headset me-2"></i>¿Necesitas acceso?</h6>
                    <p class="mb-2">Compra acceso con nuestros administradores:</p>
                    <div class="support-links">
                        <a href="https://t.me/choco_tete" target="_blank">@choco_tete</a>
                        <a href="https://t.me/WinniePoohOFC" target="_blank">@WinniePoohOFC</a>
                        <a href="https://t.me/zGatoO" target="_blank">@zGatoO</a>
                    </div>
                </div>
            </div>
        <% } %>
        
        <form method="POST" action="/login">
            <div class="mb-3">
                <label for="username" class="form-label neon-purple">
                    <i class="fas fa-user me-2"></i>Usuario
                </label>
                <input type="text" class="form-control" id="username" name="username" required>
            </div>
            
            <div class="mb-4">
                <label for="password" class="form-label neon-purple">
                    <i class="fas fa-lock me-2"></i>Contraseña
                </label>
                <input type="password" class="form-control" id="password" name="password" required>
            </div>
            
            <div class="d-grid">
                <button type="submit" class="btn btn-primary btn-lg">
                    <i class="fas fa-sign-in-alt me-2"></i>Iniciar Sesión
                </button>
            </div>
        </form>
        
        <div class="text-center mt-4">
            <div class="support-info">
                <h6 class="neon-white mb-3">¿Necesitas ayuda?</h6>
                <div class="support-links">
                    <a href="https://t.me/choco_tete" target="_blank">
                        <i class="fab fa-telegram me-1"></i>@choco_tete
                    </a>
                    <a href="https://t.me/WinniePoohOFC" target="_blank">
                        <i class="fab fa-telegram me-1"></i>@WinniePoohOFC
                    </a>
                    <a href="https://t.me/zGatoO" target="_blank">
                        <i class="fab fa-telegram me-1"></i>@zGatoO
                    </a>
                </div>
            </div>
        </div>
    </div>
    
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>