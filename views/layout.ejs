<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - Panel Admin API Keys</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        :root {
            --primary-bg: #000000;
            --secondary-bg: #1a1a1a;
            --accent-purple: #8b5cf6;
            --accent-cyan: #06b6d4;
            --text-primary: #ffffff;
            --text-secondary: #a1a1aa;
            --border-color: #374151;
        }
        
        body {
            background: var(--primary-bg);
            color: var(--text-primary);
            font-family: 'Courier New', monospace;
        }
        
        .sidebar {
            min-height: 100vh;
            background: linear-gradient(135deg, var(--primary-bg) 0%, var(--secondary-bg) 100%);
            border-right: 2px solid var(--accent-purple);
            box-shadow: 0 0 20px rgba(139, 92, 246, 0.3);
        }
        
        .sidebar .nav-link {
            color: var(--text-secondary);
            border-radius: 8px;
            margin: 2px 0;
            transition: all 0.3s;
            border: 1px solid transparent;
        }
        
        .sidebar .nav-link:hover {
            color: var(--text-primary);
            background: rgba(139, 92, 246, 0.1);
            border-color: var(--accent-purple);
            box-shadow: 0 0 10px rgba(139, 92, 246, 0.3);
        }
        
        .sidebar .nav-link.active {
            color: var(--text-primary);
            background: rgba(139, 92, 246, 0.2);
            border-color: var(--accent-purple);
            box-shadow: 0 0 15px rgba(139, 92, 246, 0.4);
        }
        
        .main-content {
            background: var(--primary-bg);
            min-height: 100vh;
        }
        
        .card {
            background: var(--secondary-bg);
            border: 1px solid var(--border-color);
            border-radius: 12px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.3);
        }
        
        .card-header {
            background: linear-gradient(135deg, var(--secondary-bg) 0%, #2a2a2a 100%);
            border-bottom: 1px solid var(--accent-purple);
            border-radius: 12px 12px 0 0 !important;
        }
        
        .btn-primary {
            background: linear-gradient(135deg, var(--accent-purple) 0%, #7c3aed 100%);
            border: none;
            border-radius: 8px;
            transition: all 0.3s;
        }
        
        .btn-primary:hover {
            background: linear-gradient(135deg, #7c3aed 0%, var(--accent-purple) 100%);
            box-shadow: 0 0 15px rgba(139, 92, 246, 0.4);
            transform: translateY(-2px);
        }
        
        .btn-danger {
            background: linear-gradient(135deg, #ef4444 0%, #dc2626 100%);
            border: none;
            border-radius: 8px;
            transition: all 0.3s;
        }
        
        .btn-danger:hover {
            background: linear-gradient(135deg, #dc2626 0%, #ef4444 100%);
            box-shadow: 0 0 15px rgba(239, 68, 68, 0.4);
            transform: translateY(-2px);
        }
        
        .table-dark {
            background: var(--secondary-bg);
            border-color: var(--border-color);
        }
        
        .table-dark th {
            background: var(--primary-bg);
            border-color: var(--accent-purple);
            color: var(--text-primary);
        }
        
        .table-dark td {
            border-color: var(--border-color);
            color: var(--text-secondary);
        }
        
        .table-dark tbody tr:hover {
            background: rgba(139, 92, 246, 0.1);
        }
        
        .neon-white {
            color: var(--text-primary);
            text-shadow: 0 0 10px rgba(255, 255, 255, 0.3);
        }
        
        .neon-purple {
            color: var(--accent-purple);
            text-shadow: 0 0 10px rgba(139, 92, 246, 0.5);
        }
        
        .neon-cyan {
            color: var(--accent-cyan);
            text-shadow: 0 0 10px rgba(6, 182, 212, 0.5);
        }
        
        .badge {
            border-radius: 6px;
            font-weight: 500;
        }
        
        .badge.bg-success {
            background: linear-gradient(135deg, #10b981 0%, #059669 100%) !important;
        }
        
        .badge.bg-danger {
            background: linear-gradient(135deg, #ef4444 0%, #dc2626 100%) !important;
        }
        
        .badge.bg-warning {
            background: linear-gradient(135deg, #f59e0b 0%, #d97706 100%) !important;
        }
        
        .form-control {
            background: var(--secondary-bg);
            border: 1px solid var(--border-color);
            color: var(--text-primary);
            border-radius: 8px;
        }
        
        .form-control:focus {
            background: var(--secondary-bg);
            border-color: var(--accent-purple);
            color: var(--text-primary);
            box-shadow: 0 0 10px rgba(139, 92, 246, 0.3);
        }
        
        .form-select {
            background: var(--secondary-bg);
            border: 1px solid var(--border-color);
            color: var(--text-primary);
            border-radius: 8px;
        }
        
        .form-select:focus {
            background: var(--secondary-bg);
            border-color: var(--accent-purple);
            color: var(--text-primary);
            box-shadow: 0 0 10px rgba(139, 92, 246, 0.3);
        }
        
        .modal-content {
            background: var(--secondary-bg);
            border: 1px solid var(--accent-purple);
            border-radius: 12px;
        }
        
        .modal-header {
            border-bottom: 1px solid var(--border-color);
        }
        
        .modal-footer {
            border-top: 1px solid var(--border-color);
        }
        
        .toast {
            background: var(--secondary-bg);
            border: 1px solid var(--accent-purple);
            color: var(--text-primary);
        }
        
        .toast-header {
            background: var(--primary-bg);
            border-bottom: 1px solid var(--border-color);
        }
        
        .navbar {
            background: var(--secondary-bg) !important;
            border-bottom: 1px solid var(--accent-purple);
        }
        
        .navbar-brand {
            color: var(--accent-purple) !important;
            font-weight: bold;
            text-shadow: 0 0 10px rgba(139, 92, 246, 0.5);
        }
        
        .navbar-nav .nav-link {
            color: var(--text-secondary) !important;
        }
        
        .navbar-nav .nav-link:hover {
            color: var(--text-primary) !important;
        }
        
        .support-bar {
            position: fixed;
            bottom: 0;
            left: 0;
            right: 0;
            background: var(--secondary-bg);
            border-top: 1px solid var(--accent-purple);
            padding: 10px;
            z-index: 1000;
            display: none;
        }
        
        .support-bar.show {
            display: block;
        }
        
        .support-links {
            display: flex;
            justify-content: center;
            gap: 20px;
        }
        
        .support-links a {
            color: var(--accent-purple);
            text-decoration: none;
            font-weight: bold;
        }
        
        .support-links a:hover {
            color: var(--text-primary);
            text-shadow: 0 0 10px rgba(139, 92, 246, 0.5);
        }
        
        .support-toggle {
            position: fixed;
            bottom: 20px;
            right: 20px;
            background: var(--accent-purple);
            color: white;
            border: none;
            border-radius: 50%;
            width: 50px;
            height: 50px;
            font-size: 20px;
            cursor: pointer;
            box-shadow: 0 4px 15px rgba(139, 92, 246, 0.4);
            transition: all 0.3s;
        }
        
        .support-toggle:hover {
            background: #7c3aed;
            transform: scale(1.1);
        }
        
        /* FORZAR TEMA HACKER - CSS con máxima prioridad */
        body, html {
            background: #000000 !important;
            color: #ffffff !important;
        }
        
        .theme-hacker {
            background: #000000 !important;
            color: #e6e6ff !important;
        }
        
        .theme-hacker .card,
        .theme-hacker .row,
        .theme-hacker .table,
        .theme-hacker .navbar,
        .theme-hacker .modal-content,
        .theme-hacker .container,
        .theme-hacker .container-fluid {
            background: linear-gradient(180deg, #0b0712, #0f0b1a) !important;
            color: #e6e6ff !important;
            border: 1px solid rgba(138,75,255,0.2) !important;
        }
        
        .row, .fila, .card, .server-row, .container, .container-fluid {
            background: linear-gradient(180deg, #07070b 0%, #0f0b1a 100%) !important;
            color: #e6e6ff !important;
            border: 1px solid rgba(255,255,255,0.06) !important;
            box-shadow: 0 6px 20px rgba(0,0,0,0.6) !important;
        }
        
        .card-header, .card-body, .card-footer {
            background: linear-gradient(180deg, rgba(12,10,20,0.9), rgba(6,4,12,0.85)) !important;
            color: #e6e6ff !important;
            border-color: rgba(138,75,255,0.08) !important;
        }
        
        .table, .table-dark, .table tbody tr, .table thead th {
            background: #0b0712 !important;
            color: #e6e6ff !important;
            border-color: rgba(138,75,255,0.08) !important;
        }
        
        .form-control, .form-select, .form-label {
            background: #0b0712 !important;
            color: #e6e6ff !important;
            border-color: rgba(138,75,255,0.08) !important;
        }
        
        .btn, .btn-primary, .btn-secondary, .btn-danger {
            background: linear-gradient(90deg, #8a4bff, #5b1bff) !important;
            color: white !important;
            border: none !important;
        }
        
        .btn-danger {
            background: linear-gradient(90deg, #ef4444, #dc2626) !important;
        }
        
        .navbar, .navbar-brand, .navbar-nav .nav-link {
            background: #0b0712 !important;
            color: #e6e6ff !important;
        }
        
        .sidebar, .sidebar .nav-link {
            background: linear-gradient(135deg, #000000 0%, #1a1a1a 100%) !important;
            color: #a1a1aa !important;
        }
        
        .sidebar .nav-link:hover, .sidebar .nav-link.active {
            background: rgba(139, 92, 246, 0.2) !important;
            color: #ffffff !important;
        }
        
        .modal-content, .modal-header, .modal-body, .modal-footer {
            background: #0b0712 !important;
            color: #e6e6ff !important;
            border-color: rgba(138,75,255,0.08) !important;
        }
        
        .toast, .toast-header, .toast-body {
            background: #0b0712 !important;
            color: #e6e6ff !important;
            border-color: rgba(138,75,255,0.08) !important;
        }
        
        .alert, .alert-info, .alert-danger, .alert-success {
            background: #0b0712 !important;
            color: #e6e6ff !important;
            border-color: rgba(138,75,255,0.08) !important;
        }
        
        .badge, .badge.bg-success, .badge.bg-danger, .badge.bg-warning {
            background: linear-gradient(90deg, #8a4bff, #5b1bff) !important;
            color: white !important;
        }
        
        .badge.bg-danger {
            background: linear-gradient(90deg, #ef4444, #dc2626) !important;
        }
        
        .badge.bg-warning {
            background: linear-gradient(90deg, #f59e0b, #d97706) !important;
        }
        
        /* Forzar todos los elementos a tema oscuro */
        * {
            box-sizing: border-box;
        }
        
        .row *, .card *, .container *, .container-fluid * {
            background: inherit !important;
        }
        
        .row .small, .card .small, .text-muted {
            color: rgba(230,230,255,0.75) !important;
        }
        
        .neon-white {
            color: #ffffff !important;
            text-shadow: 0 0 10px rgba(255, 255, 255, 0.3) !important;
        }
        
        .neon-purple {
            color: #8b5cf6 !important;
            text-shadow: 0 0 10px rgba(139, 92, 246, 0.5) !important;
        }
        
        .neon-cyan {
            color: #06b6d4 !important;
            text-shadow: 0 0 10px rgba(6, 182, 212, 0.5) !important;
        }
        
        /* ===== ANIMACIONES Y EFECTOS HACKER ===== */
        
        /* Efectos de hover mejorados */
        .btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(138, 75, 255, 0.3);
            transition: all 0.3s ease;
        }
        
        .card:hover {
            transform: translateY(-5px);
            box-shadow: 0 15px 35px rgba(0, 0, 0, 0.3);
            transition: all 0.3s ease;
        }
        
        /* Animaciones de entrada */
        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
        
        @keyframes slideInRight {
            from {
                opacity: 0;
                transform: translateX(30px);
            }
            to {
                opacity: 1;
                transform: translateX(0);
            }
        }
        
        @keyframes pulse {
            0% { transform: scale(1); }
            50% { transform: scale(1.05); }
            100% { transform: scale(1); }
        }
        
        @keyframes glow {
            0%, 100% { box-shadow: 0 0 5px rgba(138, 75, 255, 0.5); }
            50% { box-shadow: 0 0 20px rgba(138, 75, 255, 0.8); }
        }
        
        @keyframes float {
            0%, 100% {
                transform: translateY(0px) rotate(0deg);
                opacity: 0;
            }
            50% {
                opacity: 1;
            }
            100% {
                transform: translateY(-100vh) rotate(360deg);
                opacity: 0;
            }
        }
        
        @keyframes typing {
            from { width: 0; }
            to { width: 100%; }
        }
        
        @keyframes blink-caret {
            from, to { border-color: transparent; }
            50% { border-color: #8a4bff; }
        }
        
        @keyframes spin {
            to { transform: rotate(360deg); }
        }
        
        /* Aplicar animaciones */
        .card {
            animation: fadeInUp 0.6s ease-out;
        }
        
        .btn-primary {
            animation: slideInRight 0.8s ease-out;
        }
        
        .neon-cyan {
            animation: glow 2s ease-in-out infinite;
        }
        
        /* Efectos de partículas de fondo */
        .particles {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            z-index: -1;
        }
        
        .particle {
            position: absolute;
            width: 2px;
            height: 2px;
            background: rgba(138, 75, 255, 0.5);
            border-radius: 50%;
            animation: float 6s ease-in-out infinite;
        }
        
        /* Efectos de hover para tablas */
        .table tbody tr:hover {
            background: linear-gradient(90deg, rgba(138, 75, 255, 0.1), rgba(138, 75, 255, 0.05));
            transform: scale(1.01);
            transition: all 0.3s ease;
        }
        
        /* Efectos de loading */
        .loading {
            display: inline-block;
            width: 20px;
            height: 20px;
            border: 3px solid rgba(138, 75, 255, 0.3);
            border-radius: 50%;
            border-top-color: #8a4bff;
            animation: spin 1s ease-in-out infinite;
        }
        
        /* Efectos de notificación mejorados */
        .toast {
            background: linear-gradient(135deg, #0b0712, #1a0b2e);
            border: 1px solid rgba(138, 75, 255, 0.3);
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.5);
            animation: slideInRight 0.5s ease-out;
        }
        
        /* Efectos de modal mejorados */
        .modal-content {
            background: linear-gradient(135deg, #0b0712, #1a0b2e);
            border: 1px solid rgba(138, 75, 255, 0.3);
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.7);
            animation: fadeInUp 0.5s ease-out;
        }
        
        /* Efectos de input mejorados */
        .form-control:focus {
            box-shadow: 0 0 0 0.2rem rgba(138, 75, 255, 0.25);
            border-color: #8a4bff;
            animation: pulse 0.5s ease-in-out;
        }
        
        /* Efectos de badge mejorados */
        .badge {
            animation: pulse 2s ease-in-out infinite;
        }
        
        /* Efectos de iconos */
        .fas, .fab {
            transition: all 0.3s ease;
        }
        
        .fas:hover, .fab:hover {
            transform: scale(1.2) rotate(5deg);
            color: #8a4bff;
        }
        
        /* Efectos de typing para texto */
        .typing-effect {
            overflow: hidden;
            border-right: 2px solid #8a4bff;
            white-space: nowrap;
            animation: typing 3s steps(40, end), blink-caret 0.75s step-end infinite;
        }
        
        /* Animación de bienvenida */
        @keyframes welcomeSlideIn {
            0% {
                opacity: 0;
                transform: translateY(-50px) scale(0.8);
            }
            50% {
                opacity: 1;
                transform: translateY(0) scale(1.1);
            }
            100% {
                opacity: 1;
                transform: translateY(0) scale(1);
            }
        }
        
        @keyframes welcomeGlow {
            0%, 100% {
                text-shadow: 0 0 20px rgba(138, 75, 255, 0.8);
            }
            50% {
                text-shadow: 0 0 40px rgba(138, 75, 255, 1), 0 0 60px rgba(138, 75, 255, 0.8);
            }
        }
        
        .welcome-overlay {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(135deg, rgba(0, 0, 0, 0.95), rgba(11, 7, 18, 0.95));
            display: flex;
            justify-content: center;
            align-items: center;
            z-index: 9999;
            animation: fadeIn 0.5s ease-in-out;
            opacity: 1;
            visibility: visible;
        }
        
        .welcome-overlay.hidden {
            opacity: 0;
            visibility: hidden;
            transition: all 0.3s ease-out;
        }
        
        .welcome-content {
            text-align: center;
            animation: welcomeSlideIn 2s ease-out;
        }
        
        .welcome-title {
            font-size: 4rem;
            font-weight: bold;
            color: #8a4bff;
            margin-bottom: 1rem;
            animation: welcomeGlow 2s ease-in-out infinite;
        }
        
        .welcome-subtitle {
            font-size: 1.5rem;
            color: #e6e6ff;
            margin-bottom: 2rem;
        }
        
    </style>
</head>
<body class="theme-hacker">
    <!-- Partículas de fondo -->
    <div class="particles" id="particles"></div>
    
    <!-- Animación de bienvenida -->
    <div class="welcome-overlay" id="welcomeOverlay">
        <div class="welcome-content">
            <div class="welcome-title">BIENVENIDO</div>
            <div class="welcome-subtitle" id="welcomeUsername">USUARIO</div>
            <button onclick="hideWelcome()" style="margin-top: 20px; padding: 10px 20px; background: #8a4bff; color: white; border: none; border-radius: 5px; cursor: pointer;">Continuar</button>
        </div>
    </div>
    
    <div class="container-fluid">
        <div class="row">
            <!-- Sidebar -->
            <div class="col-md-3 col-lg-2 px-0">
                <div class="sidebar">
                    <div class="p-3">
                        <h4 class="neon-purple mb-4">
                            <i class="fas fa-shield-alt me-2"></i>
                            WolfData Admin
                        </h4>
                        <nav class="nav flex-column">
                            <a class="nav-link" href="/dashboard">
                                <i class="fas fa-tachometer-alt me-2"></i>
                                Dashboard
                            </a>
                            <hr class="my-2" style="border-color: var(--border-color);">
                            <h6 class="neon-cyan mb-2">API Keys</h6>
                            <a class="nav-link" href="/api-keys/dni">
                                <i class="fas fa-id-card me-2"></i>
                                /dni - DNI Básico
                            </a>
                            <a class="nav-link" href="/api-keys/dnit">
                                <i class="fas fa-id-card me-2"></i>
                                /dnit - DNI Detallado
                            </a>
                            <a class="nav-link" href="/api-keys/nombres">
                                <i class="fas fa-search me-2"></i>
                                /nombres - Búsqueda
                            </a>
                            <a class="nav-link" href="/api-keys/certificados">
                                <i class="fas fa-certificate me-2"></i>
                                /certificados - Certificados
                            </a>
                            <a class="nav-link" href="/api-keys/arbol">
                                <i class="fas fa-sitemap me-2"></i>
                                /arbol - Árbol Genealógico
                            </a>
                            <hr class="my-2" style="border-color: var(--border-color);">
                            <a class="nav-link" href="/users">
                                <i class="fas fa-users me-2"></i>
                                Usuarios
                            </a>
                            <a class="nav-link" href="/logout">
                                <i class="fas fa-sign-out-alt me-2"></i>
                                Cerrar Sesión
                            </a>
                        </nav>
                    </div>
                </div>
            </div>
            
            <!-- Main Content -->
            <div class="col-md-9 col-lg-10">
                <div class="main-content p-4">
                    <!-- Navbar -->
                    <nav class="navbar navbar-expand-lg mb-4">
                        <div class="container-fluid">
                            <span class="navbar-brand">
                                <i class="fas fa-key me-2"></i>
                                <%= title %>
                            </span>
                            <div class="navbar-nav ms-auto">
                                <span class="nav-item nav-link">
                                    <i class="fas fa-user me-2"></i>
                                    <%= username %>
                                </span>
                            </div>
                        </div>
                    </nav>
                    
                    <!-- Page Content -->
                    <% if (body === 'dashboard') { %>
                        <%- include('dashboard') %>
                    <% } else if (body === 'api-keys') { %>
                        <%- include('api-keys') %>
                    <% } else if (body === 'users') { %>
                        <%- include('users') %>
                    <% } else { %>
                        <%- body %>
                    <% } %>
                </div>
            </div>
        </div>
    </div>
    
    <!-- Support Bar -->
    <div class="support-bar" id="supportBar">
        <div class="support-links">
            <a href="https://t.me/choco_tete" target="_blank">
                <i class="fab fa-telegram me-1"></i>
                @choco_tete
            </a>
            <a href="https://t.me/WinniePoohOFC" target="_blank">
                <i class="fab fa-telegram me-1"></i>
                @WinniePoohOFC
            </a>
            <a href="https://t.me/zGatoO" target="_blank">
                <i class="fab fa-telegram me-1"></i>
                @zGatoO
            </a>
        </div>
    </div>
    
    <!-- Support Toggle Button -->
    <button class="support-toggle" onclick="toggleSupport()">
        <i class="fas fa-question"></i>
    </button>
    
    <!-- Toast Container -->
    <div class="toast-container position-fixed top-0 end-0 p-3" id="toastContainer"></div>
    
    <!-- Scripts -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        // Funciones de toast
        function showToast(message, type = 'info') {
            const toastId = 'toast-' + Date.now();
            const toastHtml = `
                <div class="toast" id="${toastId}" role="alert" aria-live="assertive" aria-atomic="true">
                    <div class="toast-header">
                        <i class="fas fa-${type === 'success' ? 'check-circle text-success' : type === 'error' ? 'exclamation-circle text-danger' : type === 'warning' ? 'exclamation-triangle text-warning' : 'info-circle text-info'} me-2"></i>
                        <strong class="me-auto">${type === 'success' ? 'Éxito' : type === 'error' ? 'Error' : type === 'warning' ? 'Advertencia' : 'Información'}</strong>
                        <button type="button" class="btn-close" data-bs-dismiss="toast" aria-label="Close"></button>
                    </div>
                    <div class="toast-body">
                        ${message}
                    </div>
                </div>
            `;
            
            document.getElementById('toastContainer').insertAdjacentHTML('beforeend', toastHtml);
            const toastElement = document.getElementById(toastId);
            const toast = new bootstrap.Toast(toastElement);
            toast.show();
            
            // Remover el toast del DOM después de que se oculte
            toastElement.addEventListener('hidden.bs.toast', function() {
                toastElement.remove();
            });
        }
        
        function showSuccess(message) {
            showToast(message, 'success');
        }
        
        function showError(message) {
            showToast(message, 'error');
        }
        
        function showWarning(message) {
            showToast(message, 'warning');
        }
        
        // Función para toggle del support bar
        function toggleSupport() {
            const supportBar = document.getElementById('supportBar');
            supportBar.classList.toggle('show');
        }
        
        // Funciones para el dashboard
        function refreshStats() {
            const now = new Date();
            const lastUpdateElement = document.getElementById('last-update');
            if (lastUpdateElement) {
                lastUpdateElement.textContent = now.toLocaleTimeString();
            }
        }
        
        // Actualizar estadísticas cada 30 segundos
        setInterval(refreshStats, 30000);
        
        // Cargar estadísticas iniciales
        document.addEventListener('DOMContentLoaded', function() {
            if (window.location.pathname === '/dashboard') {
                refreshStats();
                
                // Mostrar animación de bienvenida solo en dashboard
                if (shouldShowWelcome()) {
                    showWelcomeAnimation();
                }
            }
            
            // Crear partículas de fondo
            createParticles();
        });
        
        // Función para crear partículas de fondo
        function createParticles() {
            const particlesContainer = document.getElementById('particles');
            const particleCount = 50;
            
            for (let i = 0; i < particleCount; i++) {
                const particle = document.createElement('div');
                particle.className = 'particle';
                
                // Posición aleatoria
                particle.style.left = Math.random() * 100 + '%';
                particle.style.top = Math.random() * 100 + '%';
                
                // Tamaño aleatorio
                const size = Math.random() * 3 + 1;
                particle.style.width = size + 'px';
                particle.style.height = size + 'px';
                
                // Delay aleatorio para la animación
                particle.style.animationDelay = Math.random() * 6 + 's';
                particle.style.animationDuration = (Math.random() * 3 + 3) + 's';
                
                particlesContainer.appendChild(particle);
            }
        }
        
        // Función para verificar si debe mostrar la animación de bienvenida
        function shouldShowWelcome() {
            // Verificar si ya se mostró la animación en esta sesión
            const welcomeShown = sessionStorage.getItem('welcomeShown');
            
            // Si no se ha mostrado, marcarlo como mostrado y devolver true
            if (!welcomeShown) {
                sessionStorage.setItem('welcomeShown', 'true');
                return true;
            }
            
            return false;
        }
        
        // Función para mostrar animación de bienvenida
        function showWelcomeAnimation() {
            const welcomeOverlay = document.getElementById('welcomeOverlay');
            const welcomeUsername = document.getElementById('welcomeUsername');
            
            // Obtener nombre de usuario real
            const username = '<%= username || "Usuario" %>';
            welcomeUsername.textContent = username.toUpperCase();
            
            // Duración fija de 1 segundo (más rápido)
            const duration = 1000;
            
            // Mostrar animación por tiempo fijo
            setTimeout(() => {
                hideWelcome();
            }, duration);
        }
        
        // Función para ocultar el mensaje de bienvenida
        function hideWelcome() {
            const welcomeOverlay = document.getElementById('welcomeOverlay');
            welcomeOverlay.classList.add('hidden');
            
            // Ocultar completamente después de la transición
            setTimeout(() => {
                welcomeOverlay.style.display = 'none';
            }, 300);
        }
    </script>
</body>
</html>
